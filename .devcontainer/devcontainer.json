{
    "name": "EventManager",

    "dockerComposeFile": [
        "../docker-compose.yml",
        "docker-compose.yml"
    ],

    "service": "php",

    "workspaceFolder": "/var/www",

    "customizations": {
        "vscode": {
            "extensions": [
                "bmewburn.vscode-intelephense-client"
            ]
        }
    },

    "shutdownAction": "none"
}